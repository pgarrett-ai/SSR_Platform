# secrets — API keys live in .env files, never commit
.env
**/.env

# python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# node
node_modules/
dist/

# runtime state (SQLite DBs, trained model bundles incl. .prev backups, launch logs)
*.db
*.joblib
*.joblib.*
*.pkl
.logs/
platform/backend/app/data/runtime_settings.json
platform/backend/app/data/worker_heartbeat.json
platform/backend/app/data/worker.lock

# editor/os noise
.DS_Store
Thumbs.db
.claude/
.obsidian/
.~lock*#

# project instructions (local, not part of the public product)
/CLAUDE.md

# internal working material — never public (see memory: repo = product only)
#   docs/      cheatsheets, workflows, plans, handoffs, guides
#   research/  memos, daily sweeps, watchlist + tracker
#   archive/   git bundles, source office docs
/docs/
/research/
/archive/

# pre-platform legacy apps (superseded by platform/; kept on disk for reference only)
/capstack/
/hazard/
/fulcrum/

# stray office docs / bundles anywhere (belt-and-suspenders over the dir ignores above)
*.docx
*.xlsx
*.bundle

# bulk source data drops (SEC N-PORT quarterly ZIPs etc.) — hundreds of MB, never commit
*.zip
platform/backend/data/

# Florida-UCLA-LoPucki BRD Cases table (operator-supplied raw CSV; derived sd_events.json IS committed)
platform/backend/app/hazard/data/lopucki_brd_cases.csv

# hazard day-cache: derived, valid one day — never committed (hero overview caches ARE committed)
platform/backend/app/cache/hazard/
route_audit.csv
.superpowers/
